.DS_Store
__pycache__/
.venv/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*


# Demo program `write ... to "X.json"` outputs (money.planes -> invoice.json,
# ordinary -> big.json, pypi/net -> packages.json, rule demos -> refunds/
# results/processed.json, cache demos -> cache/config.json). Build artifacts,
# not source.
big.json
invoice.json
packages.json
refunds.json
results.json
processed.json
cache.json
config.json

# CI runner logs, timings, and the leak record (scripts/run_suites.py).
.ci-logs/
