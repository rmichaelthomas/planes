# core-sufficiency — verification

Generated by `scripts/verify-core-sufficiency.mjs` (a throwaway; the retirement rule applies).
Base for the byte-identity comparison: `90d5ae9`.

| group | assertion | result | blocking | detail |
|-------|-----------|--------|----------|--------|
| A | a `let` program refuses under --core | PASS | yes |  |
| A | the refusal names the construct | PASS | yes | got let |
| A | the refusal names the file | PASS | yes | got /var/folders/r9/60ct76ks20z0_j2dznkytq940000gn/T/core-suff-rSY4c7/uses_let.planes |
| A | the refusal names the line, exactly | PASS | yes | got line 3, approximate=false |
| A | nothing ran before the refusal | PASS | yes | output=[] |
| A | the same program runs clean with the flag OFF | PASS | yes | ["12"] |
| A | a `let` in an UNCALLED function does not refuse (runtime, not static) | PASS | yes | ["1"] |
| B | seven effect kinds performed, none flagged | PASS | no | performed ask, clock, env, random, read, show, write |
| C | meta lex (118 files) is byte-identical to 90d5ae9 | PASS | yes | 1182200 bytes here, 1182200 there |
| C | meta parse (71 files) is byte-identical to 90d5ae9 | PASS | yes | 473207 bytes here, 473207 there |
| C | meta run (71 files) is byte-identical to 90d5ae9 | PASS | yes | 5219 bytes here, 5219 there |
| D | the same core keyword set | PASS | yes | js 28, py 28 |
| D | the same core builtin set | PASS | yes | js 11, py 11 |
| D | every one of the 32 keywords classified the same | PASS | yes |  |
| D | every one of the 13 builtins classified the same | PASS | yes |  |
| D | the node -> keyword map covers every keyword (nothing is invisible) | PASS | yes |  |
| E | grammar/interp.planes | PASS | no | conforms — nothing outside the core reached |
| E | grammar/vocabulary.planes | PASS | no | conforms — nothing outside the core reached |
| E | grammar/lexer.planes | PASS | no | non-core keywords [when], builtins [] |
| E | grammar/parser.planes | PASS | no | conforms — nothing outside the core reached |
| E | grammar/json.planes | PASS | no | conforms — nothing outside the core reached |
| E | the restricted run of interp.planes + its graph completed | note | no | it refused — see the report; this is the finding, not a failure |
| F | A fails when the restriction is removed | PASS | yes |  |
| F | A fails when `let` is moved INTO the core (the map reads core.json) | PASS | yes |  |
| F | B fails when a construct it needs leaves the core | PASS | yes |  |
| F | C fails when the interpreter's behaviour actually changes | PASS | yes | off matches, on differs |
| F | D fails when the two readers are given different cores | PASS | yes | the comparison is on the loaded documents, not on a constant |
| F | E reports nothing once `when` is inside the core, and the run completes | PASS | yes | census 0 entr(ies); strict completed |

## E — the module graph, per file

| file | non-core keywords reached | non-core builtins reached | ran to completion |
|------|---------------------------|---------------------------|-------------------|
| `grammar/interp.planes` | — | — | yes |
| `grammar/vocabulary.planes` | — | — | yes |
| `grammar/lexer.planes` | `when` | — | **no** |
| `grammar/parser.planes` | — | — | yes |
| `grammar/json.planes` | — | — | yes |

**27/28 assertions passed; 0 blocking failure(s).**

