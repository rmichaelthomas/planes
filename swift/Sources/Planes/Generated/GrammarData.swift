// GENERATED by scripts/swift_grammar_gen.py — do not hand-edit.
//
// The grammar's JSON data, embedded verbatim: the files js/loader_node.mjs
// reads off disk, carried inside the library so it works without this repo.
// Regenerate with `python3 scripts/swift_grammar_gen.py`; `--check` (run by
// test_swift_grammar_data.py) fails when this file is stale. Grammar.swift
// checks each text's length and hash, then parses it once at first use.

enum EmbeddedGrammar {
    static let vocabulary = EmbeddedGrammarFile(
        path: "grammar/vocabulary.json",
        utf8Count: 19255,
        fnv1a64: 0x106A22A40E3AE7E8,
        text: ##"""
{
  "format": 1,
  "language": "planes",
  "token_classes_note": "Order is load-bearing: FINGERPRINT must precede NUMBER (most fingerprints start with a digit, and NUMBER would otherwise split \"@3f9c2d\" into a number and a name at the first letter), and OP must follow NAME.",
  "token_classes": [
    {
      "name": "COMMENT",
      "pattern": "#[^\\n]*",
      "note": "a line comment, from # to end of line"
    },
    {
      "name": "FINGERPRINT",
      "pattern": "@[0-9a-fA-F]{6}",
      "note": "A rule fingerprint -- @ plus exactly six hex characters -- has to be its own token, ahead of NUMBER and NAME: most fingerprints start with a digit, and NUMBER would otherwise split \"@3f9c2d\" into a number and a name at the first letter."
    },
    {
      "name": "NUMBER",
      "pattern": "\\d+(\\.\\d+)?",
      "note": "an integer or decimal literal; parsed exactly, never as a float"
    },
    {
      "name": "STRING",
      "pattern": "\"(?:\\\\.|[^\"\\\\])*\"",
      "note": "a double-quoted string; four escape sequences are recognized inside it -- \\\" \\\\ \\n \\t -- resolved to their code point at token construction (lexer.py), no others; the regex here only finds the string's extent (an escaped quote does not close it, so a trailing backslash right before what looks like a closing quote leaves the string unterminated), it does not itself validate which two-character escape is legal"
    },
    {
      "name": "NAME",
      "pattern": "[A-Za-z_][A-Za-z0-9_]*(-[A-Za-z0-9_]+)*",
      "note": "an identifier, hyphen-joined segments allowed (e.g. derives-from)"
    },
    {
      "name": "OP",
      "pattern": "->|==|!=|<=|>=|[+\\-*/=<>().,;:\\[\\]{}@]",
      "note": "`@` also appears in OP, as a fallback: a malformed fingerprint (wrong length, non-hex characters) then tokenizes as a lone '@' the parser can catch and name the fix for, rather than silently vanishing."
    },
    {
      "name": "WS",
      "pattern": "[ \\t]+",
      "note": "horizontal whitespace, discarded"
    }
  ],
  "keyword_categories": {
    "statement-shape": "the shape of a statement -- the parser must see these to know what kind of statement it is reading",
    "control-flow": "branching and iteration",
    "operators-and-connectives": "connectives and operator-like words that are not spellable as punctuation",
    "literals": "the three literal values that are words rather than syntax",
    "distinctive-syntax": "operations with distinctive syntax the parser must recognise",
    "rule-plane": "a compile-time-only constraint, never executed (unbound v2.0 section 33)"
  },
  "keywords_note": "Reserved words are only those that carry structure: a word the parser must see to know the shape of a statement. Everything else that used to be a keyword -- count, text, lower, upper, whole, ask, read -- is now an ordinary builtin function, callable as `count of xs` exactly like a user's own `detail of id`, and shadowable by a user function of the same name. test_names.py enforces the ceiling: every word NOT in this set must work as a function name, and the ceiling itself (32) is argued word-by-word in that file's docstring.",
  "keywords": [
    {
      "word": "to",
      "category": "statement-shape"
    },
    {
      "word": "give",
      "category": "statement-shape"
    },
    {
      "word": "let",
      "category": "statement-shape"
    },
    {
      "word": "use",
      "category": "statement-shape"
    },
    {
      "word": "show",
      "category": "statement-shape"
    },
    {
      "word": "write",
      "category": "statement-shape"
    },
    {
      "word": "why",
      "category": "statement-shape"
    },
    {
      "word": "if",
      "category": "control-flow"
    },
    {
      "word": "else",
      "category": "control-flow"
    },
    {
      "word": "for",
      "category": "control-flow"
    },
    {
      "word": "each",
      "category": "control-flow"
    },
    {
      "word": "in",
      "category": "control-flow"
    },
    {
      "word": "where",
      "category": "control-flow"
    },
    {
      "word": "when",
      "category": "control-flow"
    },
    {
      "word": "and",
      "category": "operators-and-connectives"
    },
    {
      "word": "or",
      "category": "operators-and-connectives"
    },
    {
      "word": "not",
      "category": "operators-and-connectives"
    },
    {
      "word": "of",
      "category": "operators-and-connectives"
    },
    {
      "word": "as",
      "category": "operators-and-connectives"
    },
    {
      "word": "fail",
      "category": "operators-and-connectives"
    },
    {
      "word": "with",
      "category": "operators-and-connectives"
    },
    {
      "word": "plus",
      "category": "operators-and-connectives"
    },
    {
      "word": "foreign",
      "category": "operators-and-connectives"
    },
    {
      "word": "from",
      "category": "operators-and-connectives"
    },
    {
      "word": "doing",
      "category": "operators-and-connectives"
    },
    {
      "word": "true",
      "category": "literals"
    },
    {
      "word": "false",
      "category": "literals"
    },
    {
      "word": "nothing",
      "category": "literals"
    },
    {
      "word": "first",
      "category": "distinctive-syntax"
    },
    {
      "word": "round",
      "category": "distinctive-syntax"
    },
    {
      "word": "places",
      "category": "distinctive-syntax"
    },
    {
      "word": "rule",
      "category": "rule-plane"
    }
  ],
  "builtins_note": "Builtins are ordinary functions, not keywords. The parser only needs their names so a bare `count of xs` is read as a call rather than a variable; it does not need to know what they do. A user may define a function with the same name, and theirs is the one that runs (test_names.py's test_user_definition_shadows_a_builtin). Every builtin in this language is unary -- one argument, via `of` or juxtaposition -- so arity is 1 across the board.",
  "builtins": [
    {
      "name": "count",
      "arity": 1,
      "note": "shadowable by a user function of the same name"
    },
    {
      "name": "lower",
      "arity": 1,
      "note": "shadowable by a user function of the same name"
    },
    {
      "name": "upper",
      "arity": 1,
      "note": "shadowable by a user function of the same name"
    },
    {
      "name": "text",
      "arity": 1,
      "note": "shadowable by a user function of the same name"
    },
    {
      "name": "whole",
      "arity": 1,
      "note": "shadowable by a user function of the same name"
    },
    {
      "name": "ask",
      "arity": 1,
      "note": "shadowable by a user function of the same name"
    },
    {
      "name": "read",
      "arity": 1,
      "note": "shadowable by a user function of the same name"
    },
    {
      "name": "normalize",
      "arity": 1,
      "note": "shadowable by a user function of the same name; NFC form; never an effect kind"
    },
    {
      "name": "join",
      "arity": 1,
      "note": "concatenates a list of text into one string, in order, in O(n); no separator argument (a separator is for-each's job); an empty list joins to the empty string; a non-text element is an error, never a silent coercion; shadowable by a user function of the same name; never an effect kind"
    },
    {
      "name": "sine",
      "arity": 1,
      "note": "shadowable by a user function of the same name; takes DEGREES, consistent with the drawing protocol's `rotate` -- degrees are whole numbers and stay exact under this language's arithmetic, where radians would arrive already approximated; it returns an approximate value for EVERY input including `sine of 0` -- the operation's true result being representable at one argument does not make the operation exact, which is what distinguishes it from `root`, the other operation in `value_properties.exactness.introduced_by`"
    },
    {
      "name": "rest",
      "arity": 1,
      "note": "the list without its first element; lists only (for a text prefix use `first n of`); the rest of an empty list is an error, not a silent empty list; shadowable by a user function of the same name; never an effect kind"
    },
    {
      "name": "number",
      "arity": 1,
      "note": "text to an exact number (A-Q19) -- an optional leading -, digits, and at most one .; no exponent notation, matching what a source NUMBER token can itself be; a ~-prefixed text (the language's own marker for a rounded display) is refused with its own reason rather than parsed into a different value; shadowable by a user function of the same name; never an effect kind"
    },
    {
      "name": "root",
      "arity": 1,
      "note": "square root (square-root-spec.md, closing §253) -- the first operation whose exactness is decided by its ARGUMENT rather than by itself: `root of 9` is exactly 3 and `root of 2` is approximate, because sqrt(n/d) of a reduced fraction is rational exactly when n and d are both perfect squares and the exact answer is then integer arithmetic. This is deliberately unlike `sine`, which approximates at EVERY argument because its algorithm has no exact path at any of them. A negative argument is refused, tagged not-a-number: this language has no imaginary number and inventing one for a single builtin is a larger decision. Shadowable by a user function of the same name; never an effect kind"
    }
  ],
  "effect_kinds_note": "The closed vocabulary of effect kinds, grouped by boundary. Lives here, rather than in shapes.py where it originated, because the parser also needs it -- to validate a rule's effect kind at parse time -- and parser.py cannot import shapes.py (shapes.py imports parser.py; the reverse would be a cycle). Closed is the point: an open vocabulary cannot be searched or diffed across packages, and duplicating it in two files would let the two copies drift. Grew from four kinds to seven in the FFI session of July 23, 2026 (clock, random, env), and from seven to eight for Sprint B's send effect (v37.1 addendum, Track 0 #7-#11): a foreign declaration's `doing` claim, like `clock`/`random`/`env`, never a builtin or keyword, and never callable natively -- `send` stays usable as an ordinary function name.",
  "effect_kinds": [
    {
      "kind": "ask",
      "boundary": "network",
      "note": "a request expecting a response -- fetch-only"
    },
    {
      "kind": "send",
      "boundary": "network",
      "note": "a request that carries the program's data out (a message, form, upload or report)"
    },
    {
      "kind": "read",
      "boundary": "file",
      "note": ""
    },
    {
      "kind": "write",
      "boundary": "file",
      "note": ""
    },
    {
      "kind": "show",
      "boundary": "console",
      "note": ""
    },
    {
      "kind": "clock",
      "boundary": "ambient",
      "note": "the current time"
    },
    {
      "kind": "random",
      "boundary": "ambient",
      "note": "entropy"
    },
    {
      "kind": "env",
      "boundary": "ambient",
      "note": "environment variables, process arguments"
    }
  ],
  "field_name_token_kinds_note": "Field names a record literal, with-clause, or when-pattern entry may use -- a field name is not a position where a keyword can be structural, since records nest arbitrarily (v2.0 section 35) and a field may be named `first`, `to`, or any other ordinary word that happens to double as a keyword elsewhere. NAME is included: a field may always be an ordinary, non-reserved word.",
  "field_name_token_kinds": [
    "NAME",
    "SHOW",
    "WRITE",
    "FIRST",
    "ROUND",
    "TO",
    "IN",
    "WHERE",
    "FROM",
    "AS",
    "OF",
    "USE",
    "WHY",
    "GIVE"
  ],
  "positional_words_note": "Seven words are load-bearing grammar, read positionally at exactly one site each, and deliberately kept OUT of `keywords` above to protect the reserved-word ceiling test_names.py enforces. This section is descriptive only -- the parser does not consult it; each word is still matched positionally in its own parse_* method, exactly as before. It exists because no other table in the repository records these words as grammar at all.",
  "positional_words": [
    {
      "word": "may",
      "read_at": "parser.py:parse_rule",
      "attaches_to": "a rule's assertion -- `may not` (forbid) vs `may` (permit)",
      "reserved": false,
      "note": "Neither `may` nor `not` is reserved for this: `not` already carried logical negation and needed no new grant, and `may` is read positionally inside parse_rule only, so a program that never writes `rule` still has `may` free as an ordinary name."
    },
    {
      "word": "is",
      "read_at": "parser.py:parse_comparison, parser.py:parse_when",
      "attaches_to": "`is nothing` (a comparison); `when subject is { ... }` (shape dispatch)",
      "reserved": false,
      "note": "`is` is read positionally, right here, only -- like `may` in parse_rule -- so a program that never writes `is nothing` still has `is` free as an ordinary name. Requiring NOTHING to follow (rather than accepting bare `is` unconditionally) is what lets `when subject is { ... }` use the same word positionally, one level up in parse_when, without parse_comparison's loop swallowing it first."
    },
    {
      "word": "because",
      "read_at": "parser.py:parse_because",
      "attaches_to": "a trailing rationale on an Assign or Rule",
      "reserved": false,
      "note": "read positionally, right here, only -- like `may` and `is` -- so it stays free as an ordinary name everywhere else (test_names.py's reserved-word ceiling; test_annotation.py confirms `because` stays out of KEYWORDS)."
    },
    {
      "word": "supersedes",
      "read_at": "parser.py:parse_rule",
      "attaches_to": "a rule's override clause, naming the earlier rule it replaces",
      "reserved": false,
      "note": "matched via `self.at(\"NAME\", \"supersedes\")`; positional like the others, so it never needed a reservation."
    },
    {
      "word": "contradicts",
      "read_at": "parser.py:parse_rule",
      "attaches_to": "a rule's incompatibility clause, naming the other rule it must never both apply with (B3, Track 0 #5)",
      "reserved": false,
      "note": "matched via `self.at(\"NAME\", \"contradicts\")`, read after `supersedes` (fixed order); positional like the others, so it never needed a reservation."
    },
    {
      "word": "note",
      "read_at": "parser.py:parse_statement",
      "attaches_to": "a standalone Note annotation block",
      "reserved": false,
      "note": "matched by peeking for the literal word `note` immediately followed by `:`; test_annotation.py confirms `note` stays out of KEYWORDS."
    },
    {
      "word": "derives-from",
      "read_at": "parser.py:parse_note_entry",
      "attaches_to": "one entry inside a note block, naming the rule it derives from",
      "reserved": false,
      "note": "matched via `self.at(\"NAME\", \"derives-from\")`; positional like the others."
    }
  ],
  "value_properties_note": "Descriptive only -- no loader consults this section; it exists because no other table in the repository records these rules as data at all. Every value carries whether it is exact or approximate. The rules below are enforced in all three implementations and agreed by the three-way suite.",
  "value_properties": [
    {
      "property": "exactness",
      "values": [
        "exact",
        "approximate"
      ],
      "default": "exact",
      "introduced_by": [
        "sine",
        "root"
      ],
      "rules": [
        {
          "rule": "entry",
          "statement": "A value is approximate when the true result of the operation that produced it cannot be represented as a rational. It is exact otherwise."
        },
        {
          "rule": "named-precision-reduction-stays-exact",
          "statement": "An operation that deliberately reduces precision does not make a value approximate. `round ... to N places` returns an exact value: not the original quantity, but the exact result of the operation asked for."
        },
        {
          "rule": "propagation",
          "statement": "exact combined with exact is exact. Any operation touching an approximate value yields approximate."
        },
        {
          "rule": "comparison",
          "statement": "Equality between two approximate values compares the underlying rationals and returns a plain answer. No epsilon and no tolerance: a tolerance nobody chose is the silent behaviour exact rationals exist to prevent."
        },
        {
          "rule": "static-derivability",
          "statement": "Whether a program produces approximate values is derivable without running it, by the same call-graph walk that computes the effect surface."
        },
        {
          "rule": "approximation-is-per-argument",
          "statement": "An operation in `introduced_by` may approximate at every argument or only at some. `sine` returns an approximate value for EVERY input, including `sine of 0`. `root of 9` is exactly 3 and `root of 2` is approximate. So exactness is a property of an operation APPLIED TO an argument, not of the operation alone, and reaching one of these operations is a promise of approximation only for the first kind."
        }
      ]
    }
  ],
  "binding_semantics_note": "Descriptive only -- no loader consults this section. The parser needs `let` to read a statement's shape and nothing more, so the difference between `let` and bare assignment is recorded nowhere else in loadable form. It is the difference between accumulating across a loop and a write that never escapes it (reports/REPORT_VALUES.md, V-Q5; test_values.py's test_summing_a_list_in_a_loop_produces_the_sum and test_let_inside_a_loop_shadows_and_does_not_escape run both sides of it). The one shape named below as a hazard is no longer silent -- it is refused outright (the discarded-write build).",
  "binding_semantics": [
    {
      "form": "NAME = expression",
      "binds": "rebinding",
      "statement": "Walks the enclosing scopes and rebinds wherever the name is already bound. Binds locally only when the name is genuinely new.",
      "use_for": "Accumulating a total across the iterations of a loop, and any assignment meant to outlive the block it is written in."
    },
    {
      "form": "let NAME = expression",
      "binds": "local",
      "statement": "Always binds in the current scope, shadowing any outer binding of the same name. A loop body is a fresh scope per iteration, so a `let` inside one is discarded when that iteration ends.",
      "use_for": "A value scoped to the block it is written in. Function parameters and loop variables always bind this way, so a parameter sharing a name with something in the closure shadows it instead of overwriting it.",
      "hazard": "`let total = total + x` inside a loop reads the outer total, binds a new local, and discards it at the end of the iteration -- but this exact shape (a `let` inside a loop whose right-hand side reads the very name it binds, where that name is already bound outside the loop) is now refused before the program runs, tagged `discarded-write`, naming the fix: drop `let` -- a bare assignment rebinds the outer name instead."
    }
  ]
}

"""##
    )
    static let amber = EmbeddedGrammarFile(
        path: "grammar/messages/amber.json",
        utf8Count: 3256,
        fnv1a64: 0x39AFFBDDF863739B,
        text: #"""
{
  "format": 1,
  "note": "Amber's refusal templates (addendum v4.2 section 69.5, ruling D5). Authored as data from the start -- parser.py renders these, it does not compose message text inline. `readings` is applied once per candidate reading and the results are joined; `headline`, `reason`, and `fix` are each applied once. Every {slot} is filled by the site that raises the refusal.",
  "templates": [
    {
      "id": "amber.multiword",
      "site": "parser.py:parse_primary (multi-word longest match)",
      "headline": "line {line}: two readings are possible here, and nothing says which",
      "readings": "  reading {letter}:  {source}\n              {gloss}",
      "reason": "both {names} are defined, so the parser will not choose between them",
      "fix": "try: parenthesise the one you mean -- `{suggestion}` -- or rename one of the functions so only one reading is possible"
    },
    {
      "id": "amber.juxtaposition",
      "site": "parser.py:parse_primary (takes_arg)",
      "headline": "line {line}: `{head} {next}` reads two ways, and nothing says which",
      "readings": "  reading {letter}:  {source}\n              {gloss}",
      "reason": "`{head}` takes an argument and `{next}` is also a defined function, so the parser will not choose between them",
      "fix": "try: `{head} ({next})` to call `{next}` first and pass its result to `{head}` -- or write them as two separate statements if you meant them apart"
    },
    {
      "id": "amber.juxtaposition.unknown_arity",
      "site": "parser.py:parse_primary (takes_arg)",
      "headline": "line {line}: whether `{subject}` takes an argument here is not known",
      "readings": "  reading {letter}:  {source}\n              {gloss}",
      "reason": "the arity of `{subject}` is not known here",
      "fix": "declare it, or call it with `of` so the argument is explicit"
    },
    {
      "id": "amber.paren_arglist",
      "site": "parser.py:paren_is_arglist",
      "headline": "line {line}: two readings are possible here, and nothing says which",
      "readings": "  reading {letter}:  {source}\n              {gloss}",
      "reason": "`{head}` takes exactly one argument, so both readings have the right shape",
      "fix": "try: `{head}({paren_expr})` with nothing after the parens on this call, or restructure so `{head}` clearly applies to just one thing"
    },
    {
      "id": "amber.paren_arglist.unknown_arity",
      "site": "parser.py:paren_is_arglist",
      "headline": "line {line}: whether `{head}` takes one argument here is not known",
      "readings": "  reading {letter}:  {source}\n              {gloss}",
      "reason": "the arity of `{head}` is not known here",
      "fix": "declare it, or call it with `of` so the argument is explicit"
    },
    {
      "id": "amber.rename_clause",
      "site": "parser.py:parse_statement (USE rename clause)",
      "headline": "line {line}: `{source}` reads two ways, and nothing says which",
      "readings": "  reading {letter}:  {source}\n              {gloss}",
      "reason": "more than one exported name matches this text, so the parser will not choose between them",
      "fix": "try: rename to match only one exported name exactly -- a shorter or longer phrase"
    }
  ]
}

"""#
    )
    static let core = EmbeddedGrammarFile(
        path: "grammar/core.json",
        utf8Count: 4177,
        fnv1a64: 0x56BFA7F95BAC7F2D,
        text: #"""
{
  "note": "The core subset -- the port surface a second host must implement to run grammar/interp.planes AND EVERYTHING IT REACHES THROUGH `use`. Declared here as a single source of truth (as grammar/vocabulary.json is for the full surface) and enforced by core_check.py, which fails when any file in that graph uses a keyword or builtin outside it. Derived from evidence: every member is a construct the graph actually uses; every excluded construct is one it provably avoids. reports/CORE_SUBSET.md is the prose derivation; this file is what the checker reads. Effect kinds are NOT listed as constraints -- all eight are core (an interpreter performs whatever it interprets, confirmed by shapes in Phase 5), so they are never flagged. `send` (B1, Sprint B) is core by the same rule though interp.planes never claims one itself -- see core_check.py's confirmation 2, which excepts it by name. This file is HAND-EDITED and the language's vocabulary is not, so core_check.py also holds the two against each other: every name here must still exist in the vocabulary, and every keyword and builtin the vocabulary declares must appear either in the core lists or in the matching `excluded_` map with a reason. A builtin that is neither core nor explained is a violation -- which is what `root` was, silently, between its addition and this check. THE WORD `graph` IN THE FIRST SENTENCE IS LOAD-BEARING AND WAS NOT ALWAYS THERE: this file once derived the core from interp.planes ALONE and excluded `when` on that basis, while grammar/lexer.planes -- which interp.planes reaches through `use parser` -- dispatched on record shape with `when` sixteen times, in the loop every character of every program passes through. A host built to the core as declared then could not have run the interpreter it was the port surface for.",
  "format": 1,
  "keywords": [
    "and", "as", "doing", "each", "else", "fail", "false", "first", "for",
    "foreign", "from", "give", "if", "in", "not", "nothing", "of", "or",
    "places", "plus", "round", "show", "to", "true", "use", "when", "where",
    "with", "write"
  ],
  "builtins": [
    "ask", "count", "join", "lower", "normalize", "number", "read", "rest",
    "text", "upper", "whole"
  ],
  "effect_kinds_all_core": true,
  "excluded_keywords": {
    "let": "plain `=` suffices everywhere; interp.planes never writes `let`",
    "rule": "a rule is a checker constraint, not an interpreter action",
    "why": "provenance query is a tool, not a language the interpreter needs; the deriv slot stays nothing (A.6)"
  },
  "excluded_builtins": {
    "sine": "reimplemented, not called: interp.planes computes the Taylor series from exact parts and marks the result approximate itself, because showing that derivation was the point of adding it -- a `sine` delegated to the host would report a property the host happened to track rather than implement one",
    "root": "reimplemented, not called, and for a second reason beyond `sine`'s: `root of 9` is exactly 3, so exactness here is decided by the ARGUMENT, and deciding it needs a number's numerator and denominator. interp.planes cannot look inside a number at all, so it RECOVERS them by continued-fraction expansion (square-root section) -- delegating would have left `root of (1 / 9)` approximate in the third implementation and exactly 1/3 in the other two"
  },
  "size": {
    "keywords": "29 of 32 -- 28 of them used by grammar/interp.planes itself and the twenty-ninth, `when`, by grammar/lexer.planes beneath it. The two numbers used to be the same, which is exactly why the difference went unnoticed: core_check.py's CORE SIZE block reports what the ENTRY FILE uses and this field declares the CORE, and while the entry file happened to use every core keyword nobody could tell the two apart. They now differ by one on purpose",
    "builtins": "11 of 13 -- interp.planes delegates every ordinary builtin to the host, `number` (A-Q19) among them; `sine` and `root` are the two exceptions, each reimplemented from exact parts rather than called, and each reason is in `excluded_builtins` where a loader can read it rather than here in prose",
    "effect_kinds": "8 of 8"
  }
}

"""#
    )
}
